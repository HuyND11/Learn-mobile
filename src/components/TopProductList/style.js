import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    topProductList: {
        marginTop: 16,
        marginBottom: 16,
    },
    topProductHeading: {
        marginBottom: 16,
        fontWeight: '700',
        fontSize: 16,
        lineHeight: 21,
        color: '#000000'
    },
})

export default styles;